services:
  - type: web
    name: tecball
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -c "from app.database import engine; from app.models import Base; Base.metadata.create_all(engine)" && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: tecball-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: SECURE_COOKIES
        value: "true"
      - key: ACCESS_TOKEN_EXPIRE_MINUTES
        value: "10080"
      - key: ALGORITHM
        value: "HS256"

databases:
  - name: tecball-db
    databaseName: tecball
    plan: free
